{
  "name": "nav-ai-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first computer-use agent desktop app",
  "main": "electron/main.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"wait-on tcp:5173 && electron .\"",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo 'Add linting when ready'",
    "electron:pack": "echo 'Add electron-builder config when ready'"
  },
  "dependencies": {
    "electron-store": "^11.0.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "concurrently": "^9.2.1",
    "electron": "^38.0.0",
    "vite": "^5.2.10",
    "wait-on": "^7.2.0"
  }
}
